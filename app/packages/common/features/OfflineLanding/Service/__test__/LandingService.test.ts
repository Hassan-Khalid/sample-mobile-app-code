// @ts-ignore

import LandingService from '../LandingService';


describe( 'LandingService', () => {
  it('should create correct instance of class', () => {
  // exercise
      const landingService = new LandingService();
  // post-conditions
    expect(landingService).toBeDefined();
  });
})
